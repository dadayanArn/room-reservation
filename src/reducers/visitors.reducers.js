const initialState = {};

export default function visitorsReducer(state = initialState, action) {
  switch (action.type) {
    case '':
      
     return state
  
    default:
      return state
  }
}